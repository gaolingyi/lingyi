@model CourseManager.Models.Websitelnfo

@using CourseManager.Models

@{
    ViewBag.Title = "Home Page";

    var siteInfoFormViewBag = (Websitelnfo)ViewBag.SiteInfo;

    var siteInfoFormViewDate = (Websitelnfo)ViewData["siteInfo"];

    var name = "GOGOGO";
}

